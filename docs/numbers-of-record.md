# Numbers of record

One table of numbers that may be quoted, one table of numbers and claims
that are retired. Rule: **a number may only appear in docs if a
committed, re-runnable harness produced it.** Anything not yet measured
on OCI is written exactly as: to be measured in Phase 2.

## Current

Every rate carries its Wilson 95% interval (computed by the committed
`eval/stats.py`).

| Metric | Number of record | Source (committed harness) |
|---|---|---|
| Grounding | **12/392 = 3.06% unsupported (Wilson 95% CI 1.8–5.3%)** — hosted baseline `j4cnp` (2026-09-05/06), judge v2, fixed retrieval (post-2026-09-04), 40 tickers. The point estimate is under the 5% gate; the interval reaches it. This is the judge-flagged rate; reweighted true-rate estimate 7.2% (CI 3.1–22.0%) (`eval/reweight_calibration.py`). Judge-v2 held-out calibration: precision 60% (9/15, CI 35.7–80.2%); population-weighted recall ~25% on the baseline run (CI 7.4–58.4%), driven by one miss in 20 judge-SUPPORTED claims, so the interval is wide. Supersedes "49% pre-fix → 0/84" (judge v1, pre-retrieval-fix), now a dated record below | grounding-eval DAG `grounding-eval-extended-j4cnp`; per-claim rows `eval/runs/j4cnp-claims.jsonl`; interval: `eval/stats.py` |
| Judge validation (v1, 50-claim sample, 2026-09-04) | Cohen's kappa 0.321; recall on UNSUPPORTED 1/9 = 11.1% (95% CI 2.0–43.5%); precision 1/3 = 33.3% (95% CI 6.1–79.2%). Human-UNSUPPORTED claims mostly filed as INFERENCE. Author-adjudicated labels, not blind (method + limitations: eval-methodology, "Judge validation") | `eval/agreement.py` on `eval/judge_validation/sample.csv` |
| **Judge validation (v2, held-out, labeled 2026-09-06)** | Cohen's kappa **0.580**; precision on UNSUPPORTED **9/15 = 60.0%** (95% CI 35.7–80.2%); **population-weighted recall on UNSUPPORTED 25.4% on `j4cnp` (95% CI 7.4–58.4%)**, 49.9% on `lsnnc` (CI 18.4–83.1%), 39.1% pooled (CI 13.0–74.0%). The sample was stratified by judge label (20 S / 15 U / 15 I) while judge-SUPPORTED is ~90% of real claims, so recall is reweighted to each run's judge-label counts; the one human-UNSUPPORTED claim among 20 judge-SUPPORTED carries most of the weight, so the interval is wide. Intervals: Monte Carlo over Jeffreys Beta posteriors per stratum. The unweighted 9/12 = 75.0% (2026-09-06) is superseded, see Retired. Blind author labels, no model consultation; 50 claims from the 40-ticker A/B, zero dev-set overlap. v2 over-flags INFERENCE as UNSUPPORTED (5 of 6 false positives) but, population-weighted, misses more than it over-flags: every v2 unsupported rate is a judge-flagged rate, and the true rate is estimated higher (eval-methodology, "Judge v2 held-out validation") | kappa and precision: `eval/agreement.py` on `eval/judge_validation/holdout_sample.csv` + `holdout_key.csv`; recall and true rates: `eval/reweight_calibration.py` with the same two files plus `--run` for `j4cnp` and `lsnnc` |
| Cost per brief | **$0.0366** (2026-09-06, post-retrieval-fix; 3-ticker mean AAPL/NVDA/JPM: $0.0282 exact + $0.0084 RAG-internal estimate; run evidence `cost_record_post_fix.json`). Observed, no fix: RAG answer synthesis over identical cached context differed across runs (XOM, 2026-08-24 sample rows 3 and 25), so token counts — and this cost — carry run-to-run variance | `scripts/cost_report.py` (`make cost-report`) |
| Critic recall on injected failures | 20/20 = 100% (95% CI 83.9–100%) on both runs (2026-09-04, runs 1 and 2). Run 2, with every flag persisted and adjudicated: raw precision vs injection tags 20/24 = 83.3% (95% CI 64.1–93.3%); **adjudicated precision 24/24 = 100% (95% CI 86.2–100%)** — all 4 off-needle flags were genuinely unsupported (3 injection cascades, 1 pre-existing), 0 false positives (adjudication table: eval-methodology, "Injected-failure check") | `eval/perturb.py` fixtures + `eval/critic_check.py`; re-runnable in CI (`critic-injection.yml`, asserts recall ≥ 0.8) |
| Cost per brief on OCI | to be measured in Phase 2 | same harness, re-run on OKE |
| vLLM serving on the A10 (throughput/latency) | to be measured in Phase 2 | `scripts/vllm_benchmark.py` against the oke-gpu deployment |

Quoting rules for the grounding number: always with the run ID, the
denominator, the Wilson interval, and the judge version with its
calibration — never a bare rate, and never a bare "0%". A judge-v2 rate
is the judge-flagged rate; where a reweighted true-rate estimate exists,
it goes beside it. Judge recall is only ever quoted population-weighted,
with its CI.

**Cost provenance and the pending synthesis change.** The $0.0366 cost of
record was measured on the committed pipeline as it stands: four Haiku
sections plus **Sonnet** synthesis (`agent/core.py`, `_synthesis_llm`).
The experiment behind the 2026-09-08 decision to move the synthesis step
(Executive Summary + Outlook) to Haiku with a tuned prompt is
`scripts/compare_synthesis.py`: a three-way Sonnet / Haiku / Haiku-tuned
comparison over AAPL, NVDA, JPM that generates the four sections once and
prints all three synthesis outputs in full for side-by-side reading (no
cache writes). It yields **no number of record** — it neither prices nor
judges the outputs. The switch is **held back**: synthesis stays bound to
Sonnet in `agent/core.py` pending a quality re-eval on the VM (the
grounding DAG against Haiku synthesis), so every accuracy number of
record above stays consistent with the shipped code. When the re-eval
passes and the switch lands, cost per brief is re-measured with
`scripts/cost_report.py` (same three tickers, same `--json-out` run
evidence) before any post-switch figure appears anywhere; until then
$0.0366 stands unchanged.

## Dated run records

Quotable with their dates; each from a committed harness. These are run
records, not headline numbers of record.

| Record | Value | Source (committed harness) |
|---|---|---|
| Grounding, pre-fix prompt vs grounding rules (2026-08-24, 10 tickers) — **judge v1, pre-retrieval-fix**; the former grounding number of record | 49% unsupported before the synthesis-prompt grounding rules (pre-harness, no recorded denominator, so no interval) → 0/84 unsupported after them (95% CI 0.0–4.4%). Judge v1 — v1 recall on UNSUPPORTED measured 1/9 against human labels, so 0/84 is a lower bound — and measured on the pipeline that indexed exhibits instead of Item 1A for most tickers. Retired as current 2026-09-23 in favor of `j4cnp` | `grounding_check.py --arms baseline` (docs/PHASE0_AUDIT.md "Fresh baseline eval"); interval: `eval/stats.py` |
| Extended baseline, 40 tickers (2026-09-05, `grounding-eval-extended-9j2dj`) | Judge v2, post-retrieval-fix index: 387 claims (357 S / 12 U / 18 I), **3.10% unsupported (95% CI 1.8–5.3%)** — point estimate below the 5% gate; the interval includes the gate. INFERENCE share 4.7% vs ~20% under v1. 40/40 tickers (the five ADRs ran without SEC context); per-ticker claims ranged 1–23; UPST the outlier at 3/14 unsupported. 27 min, est. $2.35. Judge-flagged rate (judge v2; held-out calibration: precision 60% (9/15, CI 35.7–80.2%); population-weighted recall ~25% on the baseline run (CI 7.4–58.4%), driven by one miss in 20 judge-SUPPORTED claims, so the interval is wide); no reweighted estimate is computed for this run. Superseded as baseline by `j4cnp` (rebuilt image, same rate: 3.06%, Fisher p = 1.0 — run-to-run stability) | grounding-eval DAG; artifacts `eval/runs/9j2dj-*`, per-claim rows `eval/runs/9j2dj-claims.jsonl` |
| **40-ticker A/B, hosted vs in-cluster vLLM fine-tune (2026-09-05/06)** | Judge v2, same image + index both arms: baseline `j4cnp` 12/392 = **3.06%** (CI 1.8–5.3%, gate PASSED, est. $2.36) vs local-model `lsnnc` 30/368 = **8.15%** (CI 5.8–11.4%, gate FAILED, est. $2.44), Fisher **p = 0.0023** — the first A/B that separates the arms on its own. Failure concentrates in the fine-tune-owned sections: attributed FH+RF claims 22/111 = 19.82% vs baseline 1/202 = 0.50% (p = 4.6e-10, `eval/section_attribution.py`; heuristic attribution, ~78% coverage); other sections statistically indistinguishable. Five ADRs on stock+news only, both arms. WMT the local outlier at 9/16. Both are judge-flagged rates; reweighted true-rate estimates: `j4cnp` 7.2% (CI 3.1–22.0%), `lsnnc` 9.8% (CI 5.3–24.2%) (`eval/reweight_calibration.py`; judge-v2 held-out calibration: precision 60% (9/15, CI 35.7–80.2%); population-weighted recall ~25% on the baseline run (CI 7.4–58.4%), driven by one miss in 20 judge-SUPPORTED claims, so the interval is wide). The A/B direction (3.06% vs 8.15%, p = 0.0023) and the per-section attribution stand: the same judge scored both arms, so its misses apply to both. The `USE_LOCAL_MODEL` ship-off decision rests on this A/B | grounding-eval DAG; per-claim rows `eval/runs/{j4cnp,lsnnc}-claims.jsonl`; stats `eval/stats.py`, breakdown `eval/section_attribution.py` |
| Per-section attribution of the 40-ticker A/B (2026-09-05/06, judge v2) | Each judged claim attributed to the pre-written section it restates (normalized containment, else word-overlap ≥ 0.6, else unattributed — a heuristic over paraphrased text, coverage ~78–79%; the buckets are diagnostic, the overall A/B is the measured result). **Fine-tune-owned (FH + RF):** baseline 1/202 = 0.50% (CI 0.1–2.8%) vs local-model **22/111 = 19.82%** (CI 13.5–28.2%), Fisher **p = 4.6e-10**. **Other sections:** 3/104 = 2.88% (CI 1.0–8.1%) vs 2/180 = 1.11% (CI 0.3–4.0%), p = 0.36. **Unattributed:** 8/86 = 9.30% (CI 4.8–17.3%) vs 6/77 = 7.79% (CI 3.6–16.0%), p = 0.79. The excess is concentrated entirely in fine-tune-authored content; the arms are indistinguishable elsewhere. Attribution shift: 202 baseline vs 111 local-model claims attributed to FH/RF — the fine-tune's phrasing is restated less. These are judge-flagged rates and this judge-flagged comparison is the per-section headline; both arms share the judge. Reweighted estimates: sensitivity row below | `python eval/section_attribution.py --run eval/runs/j4cnp-claims.jsonl eval/runs/raw/j4cnp-findings --run eval/runs/lsnnc-claims.jsonl eval/runs/raw/lsnnc-findings` over the committed per-claim rows and findings (re-run 2026-09-09, reproduces; prints p-values at four decimals) |
| Per-section sensitivity: reweighted true-rate estimates (2026-09-24), **not the headline** | Same buckets, baseline vs local-model: FH + RF 5.4% (CI 1.0–21.3%) vs 16.0% (CI 9.8–29.4%); other 6.7% (CI 2.3–22.2%) vs 5.8% (CI 1.4–21.5%); unattributed 12.3% (CI 7.5–24.6%) vs 10.4% (CI 6.0–23.7%). Assumes one judge-SUPPORTED miss rate (1/20 in the held-out sample) shared across both arms and all sections; the sample cannot say whether misses differ by arm or section. Under that assumption the baseline FH + RF estimate is almost entirely the assumed miss rate applied to its 199 judge-SUPPORTED claims, so the gap narrows. The headline stays the judge-flagged 0.50% vs 19.82%, p = 4.6e-10 | `python eval/reweight_calibration.py --labeled eval/judge_validation/holdout_sample.csv --key eval/judge_validation/holdout_key.csv --run j4cnp eval/runs/j4cnp-claims.jsonl eval/runs/raw/j4cnp-findings --run lsnnc eval/runs/lsnnc-claims.jsonl eval/runs/raw/lsnnc-findings --by-section` |
| Reproduction run on a rebuilt node, 10 tickers (2026-09-23, `grounding-eval-2nh8v`) — **not a number of record** | Node `vm-a10-inst-1` (VM.GPU.A10.1, rebuilt from the runbook that day); the 2026-09-03 10-ticker set (the `grounding-eval` template default, unchanged since 2026-08-24; the aggregate lists the same 10), hosted-models (`baseline`) arm, judge v2: 87 claims (81 S / 0 U / 6 I), **0/87 unsupported (95% CI 0.0–4.2%)**, gate PASSED, est. $0.61 (aggregate estimate, $0.6108). The 87 is the DAG aggregate's own count and is per-claim deduped (the same rule as `eval/label.py` `count_labels_deduped`): the AMZN judge repeated three SUPPORTED labels, which the raw `count_labels` would count (90 claims, 84 S); unsupported is 0 either way. Consistent with the 40-ticker baseline `j4cnp` (12/392 = 3.06%, CI 1.8–5.3%): intervals overlap, Fisher p = 0.14 — at n = 87 a zero count does not distinguish the rate from ~3%. 0/87 is the judge-flagged rate; reweighted true-rate estimate 5.6% (CI 1.4–20.8%, `eval/reweight_calibration.py` on `2nh8v`, applying the `j4cnp`/`lsnnc` held-out miss rates to this same-arm run), so zero flags is not zero unsupported claims. Evidence that the runbook rebuild reproduces a passing gate, not a new headline | grounding-eval DAG; aggregate `eval/runs/2nh8v-aggregate.txt`; findings `eval/runs/raw/2nh8v-findings/`; per-claim rows `eval/runs/2nh8v-claims.jsonl` (`eval/parse_run_log.py` `rows_from_findings_dir`, per-ticker counts match the aggregate on all 10); interval + p-value `eval/stats.py` |
| Four-arm model comparison, 40 tickers (2026-09-23, node `vm-a10-inst-2`) — **a dated comparison set, not a number of record** | Judge v2; arms differ only in who writes Financial Health + Risk Factors, local arms on vLLM v0.10.2 with identical pinned sampling. hosted `kcf7s` 4/383 = 1.04% (CI 0.4–2.7%, gate passed); financial-lora `v924f` 25/385 = 6.49% (CI 4.4–9.4%, failed); qwen2.5-1.5b-instruct `4nfsm` 31/400 = 7.75% (CI 5.5–10.8%, failed); qwen2.5-7b-instruct `cnkp2` 18/393 = 4.58% (CI 2.9–7.1%, passed on the point estimate only). Exact Fisher: fine-tune vs its base p = 0.58; 1.5B vs 7B base p = 0.076 (borderline); hosted vs each local arm p = 7.8e-05 / 2.6e-06 / 0.0039. Not a size curve (hosted is a different family). Serving on the A10, same settings: financial-lora 711.5 output tok/s, 1.5B base 711.3, 7B base 194.6. Judge-flagged rates (judge v2; held-out calibration: precision 60% (9/15, CI 35.7–80.2%); population-weighted recall ~25% on the baseline run (CI 7.4–58.4%), driven by one miss in 20 judge-SUPPORTED claims, so the interval is wide). No reweighted estimate: the held-out miss rates were measured on `j4cnp`/`lsnnc` claims and are not extended to other models; comparisons hold in direction because all arms share the judge. Full tables: eval-methodology, "Four-arm model comparison" | grounding-eval DAG; `eval/multi_arm_stats.py` over `eval/runs/{kcf7s,v924f,4nfsm,cnkp2}-claims.jsonl`; `scripts/vm_bench_serve.sh` → `eval/runs/bench/*.json` |
| Grounding A/B, hosted vs in-cluster vLLM fine-tune (2026-09-03, 10 tickers) | 3.03% (2/66, CI 0.8–10.4%) vs 12.31% (8/65, CI 6.4–22.5%) unsupported, Fisher p=0.0545; local-model arm failed the 5% gate, baseline passed. Judge v1; v1 recall on UNSUPPORTED measured 1/9 against human labels, so both rates are lower bounds — the A/B *direction* is unaffected because both arms used the same judge | grounding-eval DAG runs (eval-methodology dated A/B); intervals `eval/stats.py` |
| Grounding, hosted vs local-hybrid (Aug 2026, 9-ticker balanced) | 86.2% vs 77.8% (judge v1) | `grounding_check.py` A/B, benchmarks.md |
| Cost per brief, pre-retrieval-fix (Aug 2026, 3-ticker mean) | $0.0316 — the former cost of record, measured on the pre-retrieval-fix pipeline (exhibit/TOC-weighted contexts); superseded 2026-09-06 by the $0.0366 re-measure on the fixed pipeline, same N and tickers | `scripts/cost_report.py`, benchmarks.md |
| Cost, hosted vs hybrid | $0.0316 vs $0.0321 per brief (pre-retrieval-fix pipeline) — sections saving within run-to-run variance | `scripts/cost_report.py`, benchmarks.md |
| Cost harness, early run (2026-08-24, 3-ticker mean) | $0.0336/brief = $0.0272 exact (4 Haiku sections + Sonnet synthesis) + $0.0064 RAG-internal estimate — a dated run record of the pre-retrieval-fix pipeline | `scripts/cost_report.py` |
| First nightly gate fire (2026-08) | 5.62% unsupported (judge v1), one NVDA outlier draft; re-measure 0/10 → variance, threshold kept at 5% | Argo run + NVDA re-measure, README red-night playbook |
| Local CPU serving (environment-limited) | ~7.7 tok/s aggregate saturation; p50 13.3s→74.1s at concurrency 1→8; not comparable to GPU/hosted | `scripts/vllm_benchmark.py`, benchmarks.md |
| Pipeline latency | 26.29s mean (~26s) per brief | `grounding_check.py` pipeline timing, Phase 0 re-measure |
| K8s smoke test | 13/13 assertions | `scripts/k8s_smoke_test.sh` |
| Retrieval defect fix (2026-09-04) | Pre-fix: 3/40 tickers' risk retrieval passed verification (32 served exhibit/TOC text; 5 ADRs unfetchable). Post-fix: 32/40 pass; remaining 8 itemized in eval-methodology "Retrieval defect". Grounding numbers dated before 2026-09-04 measured the pipeline against exhibit text for most tickers and stand as dated records of that pipeline | `scripts/reindex_filings.py` verify pass |
| Cost harness, post-retrieval-fix preview run (2026-09-04, 3-ticker mean) | $0.0364/brief = $0.0280 exact + $0.0084 RAG-internal estimate — richer risk contexts lengthen inputs; confirmed by the 2026-09-06 re-measure that set the $0.0366 cost of record | `scripts/cost_report.py` |
| Synthesis-model comparison (2026-09-08, AAPL/NVDA/JPM) | Qualitative, side-by-side: Sonnet vs Haiku vs Haiku with a tuned prompt on the synthesis step; decision — Haiku with the tuned prompt (option C). No rate or cost is recorded from it; the switch is held back pending a quality re-eval on the VM (synthesis still Sonnet in the shipped code), so the $0.0366 cost of record and the grounding numbers are unaffected | `scripts/compare_synthesis.py` (prints all three outputs; does not write to cache) |
| Test suite | 2337 lines (`wc -l tests/*.py`), 160 tests collected: 159 passed + 1 skipped (the credit-gated judge test) — as of 2026-09-24 | `python -m pytest tests/ --collect-only`; `python -m pytest tests/` |

## Retired

| Retired number / claim | Why retired | Say instead |
|---|---|---|
| $0.0269 per brief | superseded by the committed cost harness | $0.0366 (2026-09-06, post-retrieval-fix; source above) |
| $0.0316 per brief quoted as current | measured on the pre-retrieval-fix pipeline; superseded by the 2026-09-06 re-measure | $0.0366 (2026-09-06, post-retrieval-fix); $0.0316 only as a dated pre-fix record |
| "54% cost reduction" | sections-only framing; the full-brief comparison does not support it | nothing — the claim is retired without replacement |
| bare "0% unsupported" | drops the baseline, the denominator, and the interval | the current grounding number of record (above), with run ID, CI, and judge version |
| "49% pre-fix → 0/84" as the current grounding number | measured 2026-08-24 under judge v1 (a lower bound) on the pre-2026-09-04 retrieval pipeline, which indexed exhibits for most tickers; pre-fix numbers are dated records only | `j4cnp` 12/392 = 3.06% (CI 1.8–5.3%, judge v2, fixed retrieval, 40 tickers); "49% pre-fix → 0/84" only as a dated record tagged judge v1, pre-retrieval-fix |
| Judge v2 UNSUPPORTED recall 9/12 = 75.0% (CI 46.8–91.1%), 2026-09-06 | computed on the sample as drawn; the sample was stratified by judge label (20 S / 15 U / 15 I) and never reweighted to the runs, where judge-SUPPORTED is ~90% of claims | population-weighted recall 25.4% on `j4cnp` (CI 7.4–58.4%), `eval/reweight_calibration.py` |
| "semantic cache" | the Redis cache is an exact-key cache (`research:{TICKER}`) | "exact-key cache per ticker" |
| "Celery/Redis ran in production on ECS" | ECS reality was a single FastAPI container + RDS | "K8s is the first full-topology deployment" |
| "vLLM will serve the model" as a pending claim | superseded — vLLM served the fine-tune on an A10 (Docker 2026-09-02, in-cluster k3s 2026-09-03) and the eval A/Bs against it failed the gate | "vLLM served the fine-tune on an A10; the 40-ticker A/B was 3.06% (1.8–5.3%) vs 8.15% (5.8–11.4%), judge v2 (judge-flagged rates; held-out precision 60%, population-weighted recall ~25% on the baseline, CI 7.4–58.4%; direction unaffected, same judge both arms), p = 0.0023, failure concentrated in the fine-tune's own sections — ships default-off" (dated records in eval-methodology.md; OKE serving still ungated) |

## Framing rules that travel with the numbers

- Cross-encoder reranking and the multi-agent supervisor **shipped
  default-off because evals showed no grounding gain at higher
  cost/latency** — state it that way, not as unfinished work.
- When vLLM actually serves on the A10 (Phase 2), CLAUDE.md is updated
  first and the claim becomes legitimate everywhere.

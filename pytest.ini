[pytest]
# Bare `pytest` must collect only the project suite — without this it wanders
# into vendored trees (financial-lora-build/llama.cpp ships its own tests and
# breaks collection on missing dev deps).
testpaths = tests
